"use client";

export type UploadSectionProps = {
  onImageSelected: (file: File) => void;
};

// Implemented in the upload/crop step: drag-and-drop + file picker,
// accepting JPG/PNG/HEIC.
export function UploadSection(_props: UploadSectionProps) {
  return null;
}

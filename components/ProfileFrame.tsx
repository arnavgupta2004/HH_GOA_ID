export type ProfileFrameProps = {
  imageSrc: string;
};

// Implemented in the Profile Frame step: circular frame with HH Goa 2026
// branding, gradients, and palm-inspired accents.
export function ProfileFrame(_props: ProfileFrameProps) {
  return null;
}

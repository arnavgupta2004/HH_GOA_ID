export type BuilderCardProps = {
  imageSrc: string;
  name: string;
  role: string;
  builderTitle: string;
  stack: string[];
};

// Implemented in the Builder Card step: photo, name, role, generated
// title, stack chips, HH Goa logo, QR placeholder, decorative graphics.
export function BuilderCard(_props: BuilderCardProps) {
  return null;
}

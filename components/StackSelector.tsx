export type StackSelectorProps = {
  value: string[];
  onChange: (value: string[]) => void;
};

// Implemented in the Builder Card step: chip input with suggestions
// (Python, TypeScript, Rust, ...) and custom values.
export function StackSelector(_props: StackSelectorProps) {
  return null;
}
